export const Framework = ({
  icon,
  heading,
  description,
  url,
  iconColor,
  color,
  ...props
}) => (
  <Link
    to={url}
    d="block"
    rounded="lg"
    boxShadow="xl"
    bg={iconColor}
    target="_blank"
  >
    <Flexbox flexDirection="column" textAlign="center" p="4|6|8" {...props}>
      <Flexbox
        bg={iconColor}
        color="white"
        rounded="full"
        alignItems="center"
        justifyContent="center"
      >
        <Icon name={icon} size="12|12|18" />
      </Flexbox>
      <H3 fontSize="2xl" mt="4|6" mb="0" color={color}>
        {heading}
      </H3>
      {description && <P mb="0">{description}</P>}
    </Flexbox>
  </Link>
)
