export const ThemeReferenceNav = ({ categories, ...props }) => {
  const [nav] = useNav("theme-reference")
  return (
    <>
      <h3 variant="heading.h3" my="4" fontSize="xl">
        Theme reference
      </h3>
      <ul listStyle="none" p="0" m="0" {...props}>
        {nav.items.map(({ url, value }, index) => (
          <li key={index} mb="2">
            <a href={url} color="text" fontSize="md">
              {value}
            </a>
          </li>
        ))}
      </ul>
    </>
  )
}
