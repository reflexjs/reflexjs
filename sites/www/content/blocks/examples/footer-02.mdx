import { useSiteMetadata } from "@reflexjs/gatsby-theme-core"

export const FooterBlock = () => {
  const { title } = useSiteMetadata()
  return (
    <section py={[8, 10, 12]}>
      <Container>
        <Flexbox
          flexDirection={["column", "row"]}
          alignItems={["flex-start", "center"]}
          justifyContent="space-between"
        >
          <h2 variant="heading.h2" m="0" fontSize="xl|2xl|3xl">
            {title}
          </h2>
          <NavMenu
            name="footer-example"
            dropdown={false}
            expand={false}
            my="6|0"
            gap="4|8"
          />
          <Grid col="2" gap="4" mt={[4, 0]}>
            <a href="#" color="text">
              <Icon name="twitter" size={5} />
              <VisuallyHidden>Follow on Twitter</VisuallyHidden>
            </a>
            <a href="#" color="text">
              <Icon name="instagram" size={5} />
              <VisuallyHidden>Follow on Instagram</VisuallyHidden>
            </a>
          </Grid>
        </Flexbox>
        <div
          borderTopWidth={1}
          textAlign="center"
          pt={[4, 5, 6]}
          mt={[4, 5, 6]}
        >
          <p fontSize="sm" mt="0">
            © {new Date().getFullYear()} {title}
          </p>
        </div>
      </Container>
    </section>
  )
}
