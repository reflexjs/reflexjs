export const LibraryBlockFooter = ({ block, ...props }) => (
  <Container py="4" {...props}>
    <Block
      src={`library/doc-${block.doc}`}
      maxW={["none", "60%"]}
      sx={{
        h2: {
          fontSize: "4xl",
          a: {
            color: "text",
            textDecoration: "none",
          },
        },
      }}
    />
  </Container>
)
