export const Feature = ({
  title,
  text,
  icon = "check-circle",
  iconColor = "#27ae60",
}) => (
  <div borderWidth="1px" p="4" rounded="xl">
    <h3
      variant="heading.h3"
      fontSize="xl"
      m="0"
      display="flex"
      alignItems="center"
    >
      <Icon name={icon} mr="2" size="5" color={iconColor} />
      {title}
    </h3>
    <p fontSize="md" mb="0" mt="2">
      {text}
    </p>
  </div>
)
