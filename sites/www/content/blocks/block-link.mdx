export const BlockLink = ({ heading, text, url, links, ...props }) => (
  <div
    borderWidth="1px"
    rounded="xl"
    maxW="none|none|350px"
    bg="background"
    zIndex="500"
    position="relative"
    {...props}
  >
    <a
      href={url}
      p="4"
      display="flex"
      flexDirection="column"
      alignItems="flex-start"
      sx={{
        "&:hover": {
          h4: {
            color: "primary",
          },
        },
      }}
      _hover={{
        color: "primary",
      }}
    >
      <h4
        variant="heading.h4"
        my="0"
        display="flex"
        fontSize="xl|2xl"
        alignItems="center"
      >
        {heading}
        <Icon name="arrow-right" ml="2" size="5" />
      </h4>
      <p mt="2" mb="0" color="text" fontSize="md">
        {text}
      </p>
    </a>
    {links && (
      <Flexbox flexDirection="column" alignItems="flex-start">
        {links.map((link) => (
          <a
            href={link.url}
            px="4"
            py="2"
            fontSize="md"
            borderTopWidth="1px"
            w="full"
            color="text"
            transition="transform .25s ease-in"
            _hover={{
              color: "primary",
              transform: "translateX(5px)",
            }}
          >
            {link.title}
          </a>
        ))}
      </Flexbox>
    )}
  </div>
)
