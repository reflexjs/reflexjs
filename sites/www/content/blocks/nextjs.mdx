export const NextjsBlock = ({ mode, ...props }) => {
  return null
  if (mode === "small") {
    props.p = "0|0|0|0"
  }
  return (
    <Container mt="8|10" mb="8|0" {...props}>
      <Flexbox
        flexDirection={mode === "small" ? "column" : "column|row"}
        alignItems={mode === "small" ? "flex-start" : "flex-start|center"}
        bg="#00F5A0"
        color="#1E1D56"
        p="6"
        rounded="lg"
      >
        <p m="0" fontSize="lg|xl">
          We are adding support for <strong>Nextjs</strong>. Get in touch on{" "}
          <a href="https://twitter.com/arshadcn">Twitter</a> to learn more.{" "}
        </p>
        <a
          variant="button"
          w="180px"
          href="https://mdnext-reflexjs.vercel.app/"
          bg="#1E1D56"
          borderColor="#1E1D56"
          color="#00F5A0"
          ml={mode === "small" ? "0" : "0|auto"}
          mt={mode === "small" ? "4" : "4|0"}
          target="_blank"
        >
          See a demo
        </a>
      </Flexbox>
    </Container>
  )
}
