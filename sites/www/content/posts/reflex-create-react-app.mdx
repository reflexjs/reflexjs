---
title: How to use Reflex with Create React App
excerpt: Learn how to use Reflex components and blocks in a non-Gatsby app.
date: 2020-06-29
author: Arshad
image: reflex-create-react-app.jpg
caption: Create React App + Reflex
---

Reflex [components](/docs/components) and [blocks](/library/blocks) can be used in any React app and are not limited to Gatsby.

Here's how to use Reflex with [Create React App](https://create-react-app.dev):

**Step 1: Create a new React app**

```sh
npx create-react-app my-app
```

**Step 2: Add Reflex components and the base preset**

```sh
npm i @reflexjs/components @reflexjs/preset-base
```

**Step 3: Wrap your `<App />` in a `ThemeProvider`**

```jsx title=src/index.js
import React from "react"
import ReactDOM from "react-dom"
import { ThemeProvider } from "theme-ui"
import theme from "@reflexjs/preset-base"
import App from "./App"

const rootElement = document.getElementById("root")
ReactDOM.render(
  <React.StrictMode>
    // highlight-start
    <ThemeProvider theme={theme}>
      <App />
    </ThemeProvider>
    // highlight-end
  </React.StrictMode>,
  rootElement
)
```

You can now use Reflex components in any component.

```jsx title=src/App.js
import React from "react"
import {
  Section,
  H1,
  P,
  Button,
  Container,
} from "@reflexjs/components"

export default function App() {
  return (
    <Section py="8|12|16|24">
      <Container>
        <H1 m="0" fontWeight="extrabold" lineHeight="tight">
          Experiences that sell.
        </H1>
        <P fontSize="xl|2xl" mt="2">
          We connect you to the exclusive works from top artists around the
          world.
        </P>
        <Button variant="primary">Get Started</Button>
        </Grid>
      </Container>
    </Section>
  )
}
```

Check out the [codesandbox](https://codesandbox.io/s/trusting-panini-zvuwo) example.

<iframe
  src="https://codesandbox.io/embed/trusting-panini-zvuwo?autoresize=1&fontsize=14&hidenavigation=1&theme=dark&view=preview"
  style="width:100%; height:500px; border:0; border-radius: 4px; overflow:hidden;"
  title="trusting-panini-zvuwo"
  allow="accelerometer; ambient-light-sensor; camera; encrypted-media; geolocation; gyroscope; hid; microphone; midi; payment; usb; vr; xr-spatial-tracking"
  sandbox="allow-autoplay allow-forms allow-modals allow-popups allow-presentation allow-same-origin allow-scripts"
></iframe>
