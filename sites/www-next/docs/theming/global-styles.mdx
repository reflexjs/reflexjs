---
title: Global Styles
excerpt: Add global styles from your theme.
prev:
  title: Pseudo Selectors
  url: /docs/theming/pseudo-selectors
next:
  title: Color Modes
  url: /docs/theming/color-modes
---

Use the `styles.global` key in your theme to set theme-aware styles for any elements.

By default, the `ThemeProvider` will apply styles in `styles.global` globally and `styles.root` to the `<body>` element.

```js title=theme.js
export default {
  ...
  styles: {
    // Applies globally.
    global: {
      a: {
        cursor: "pointer",
      }
    },

    // Applies to the `body` element.
    root: {
      m: 0,
      p: 0,
    }
  }
  ...
}
```
