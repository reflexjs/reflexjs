---
title: Getting Started
excerpt: Welcome to the Reflex documentation.
---

<Callout>


**Are you using a React framework?** See the installation instructions for [create-react-app](/docs/getting-starterd/create-react-app), [Next.js](/docs/getting-started/next) or [Gatsby](/docs/getting-started/gatsby).

</Callout>


<span color="accent">sdasd</span>

<p color="primary">This is a paragraph</p>

<Box display="grid" col="1|2|3" my="8" gap="4">
  <LinkedCard href="/docs/installation/next" icon="nextjs" title="Next.js" />
  <LinkedCard
    href="/docs/installation/gatsby"
    icon="gatsby"
    title="Gatsby"
    color="#663399"
  />
  <LinkedCard
    href="/docs/installation/create-react-app"
    icon="react"
    title="Create React App"
    color="#61DAFB"
  />
</Box>

## Quick Start

### Step 1

Start by installing the `reflexjs` package into your React app.

```sh
npm i reflexjs
```

### Step 2

Create a theme with your custom colors and typography.

```js title=theme.js
export default {
  fonts: {
    body: "system-ui, sans-serif",
    heading: '"Avenir Next", sans-serif',
    monospace: "Menlo, monospace",
  },
  colors: {
    text: "#000",
    background: "#fff",
    primary: "#33e",
  },
}
```

### Step 3

Wrap your app with the `ThemeProvider` from reflexjs.

```jsx
import * as React from "react"
import theme from "./theme" // highlight-line
import { ThemeProvider } from "reflexjs" // highlight-line

export default function () {
  return (
    <ThemeProvider theme={theme}>
      <App />
    </ThemeProvider>
  )
}
```

## Styling

You are now ready to style your components using [style props](/docs/styling).

```jsx
/** @jsx jsx */
import { jsx } from "reflexjs"

export default function CustomButton({ children }) {
  return (
    <button bg="primary" color="text" p="10px" rounded="5">
      {children}
    </button>
  )
}
```
